package com.brunoapp.fittrack.presentation.theme

import androidx.compose.ui.graphics.Color

// ── FitTrack brand palette ──
// Lime green primary on charcoal background. Own identity, not copied from any app.

val LimePrimary = Color(0xFFC6F432)        // main accent
val LimeDark = Color(0xFF9BC71E)           // pressed / variant
val LimeContainer = Color(0xFF2A3312)      // containers with lime tint

val CharcoalBackground = Color(0xFF0F110E) // app background (soft black-green)
val CharcoalSurface = Color(0xFF181B16)    // cards
val CharcoalSurfaceHigh = Color(0xFF22261F) // elevated cards / sheets
val CharcoalOutline = Color(0xFF3A4034)

val TextPrimary = Color(0xFFF2F4EE)        // main text
val TextSecondary = Color(0xFF9BA394)      // secondary text
val TextDisabled = Color(0xFF5C6355)

val GoldRecord = Color(0xFFF5C542)         // personal records
val GreenSuccess = Color(0xFF4CAF50)       // completed goals
val RedError = Color(0xFFE5484D)           // destructive actions only

// Light theme counterparts
val LightBackground = Color(0xFFF7F8F4)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceHigh = Color(0xFFEDF0E8)
val LightTextPrimary = Color(0xFF1A1D17)
val LightTextSecondary = Color(0xFF5A6152)
val LimeOnLight = Color(0xFF5A7A00)        // lime readable on white

val Color_LimeContainerLight = Color(0xFFE3F2C2) // light lime container
